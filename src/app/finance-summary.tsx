const money = (value: number) => value.toLocaleString("en-US");

export default function FinanceSummary({ totalSales, commissionAmount, winningPayout, profit, commissionPercent }: { totalSales: number; commissionAmount: number; winningPayout: number; profit: number; commissionPercent: number }) {
  return <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold">ငွေစာရင်းအနှစ်ချုပ်</h2><span className="text-xs text-slate-500">လက်ရှိအကြိမ်</span></div><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-blue-50 p-3"><span className="text-blue-700">စုစုပေါင်းရောင်းအား</span><b className="mt-1 block text-base text-blue-900">{money(totalSales)}</b></div><div className="rounded-lg bg-violet-50 p-3"><span className="text-violet-700">ကော်မရှင် ({commissionPercent}%)</span><b className="mt-1 block text-base text-violet-900">{money(commissionAmount)}</b></div><div className="rounded-lg bg-red-50 p-3"><span className="text-red-700">ပေးရန်ငွေ</span><b className="mt-1 block text-base text-red-900">{money(winningPayout)}</b></div><div className={`rounded-lg p-3 ${profit >= 0 ? "bg-emerald-50" : "bg-orange-50"}`}><span className={profit >= 0 ? "text-emerald-700" : "text-orange-700"}>{profit >= 0 ? "အမြတ်" : "အရှုံး"}</span><b className={`mt-1 block text-base ${profit >= 0 ? "text-emerald-900" : "text-orange-900"}`}>{money(Math.abs(profit))}</b></div></div></section>;
}
